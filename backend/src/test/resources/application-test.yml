spring:
  datasource:
    url: jdbc:h2:mem:release-tests;DB_CLOSE_DELAY=-1
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
app:
  cors-origins: https://release.example.com
springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
